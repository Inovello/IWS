# Home page: the daily-driver strip and the "Currently open" panel.
# `stats` is whatever model I actually serve to myself right now.
# `now.items` is open work only. Anything finished belongs in a writeup, not here.

hardware_line: "dual Xeon E5-2696 v4 · 2x RTX 3090 · 192GB DDR4-2133 ECC · Ubuntu Server 24.04 · llama.cpp + vLLM"

stats:
  caption: "Daily driver · Qwen3.8-Flash-Next UD-Q4_K_XL · 261k context · expert cache + MTP · phase-memory prefill"
  items:
    - label: Decode
      value: "41"
      unit: t/s
      accent: true
    - label: Prefill
      value: "224"
      unit: t/s
    - label: VRAM
      value: "48"
      unit: GB
    - label: Host RAM
      value: "192"
      unit: GB

now:
  updated: "Sep 2026"
  line: "Prefill is a factor of four better and still spends most of its time moving the same weights more than once."
  items:
    - tag: Next
      highlight: true
      title: "Uploading each expert once per prompt"
      text: >-
        After part 4 an 8k prompt still moves 278 GB to the first GPU, 3.6 times the expert set, and that is
        72% of the time to first token. A patch that shares one staged copy across the consumers in a batch
        is written and reviewed, not built. The bar: at least 1.5x on prefill, decode within 3%, the same
        quality screen.
    - tag: PR open
      title: "llama.cpp #28671"
      url: https://github.com/ggml-org/llama.cpp/pull/28671
      text: >-
        Radix-select top-k in the CUB fallback for wide rows, the change behind part 3. Waiting on
        workflow approval. #28223 (host buffer overrides under mmap) is closed for now under the
        one-open-PR rule for new contributors and comes back after this one.
